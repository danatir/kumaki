{
  "name": "kumaki",
  "version": "1.0.0",
  "private": true,
  "description": "Static study site. This package.json exists only so Netlify installs the dependencies used by netlify/functions — deps inside a function's own package.json are not installed automatically.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
